
# LLaMA 340,825,088 (340.83M) params
# huggingface/transformers/.. models/llama/configuration_llama.py

model_type = "llama"
hidden_size = 1024         # embedding dimension
intermediate_size = 4096   # FFN hidden size (typically 4x hidden_size)
max_position_embeddings = 65536
num_attention_heads = 16   # attention heads (must divide hidden_size evenly)
num_hidden_layers = 18     # transformer layers
num_key_value_heads = 4
vocab_size = 32768
rope_scaling = {factor = 16.0, high_freq_factor = 4.0, low_freq_factor = 1.0, original_max_position_embeddings = 4096, rope_type = "llama3"}
