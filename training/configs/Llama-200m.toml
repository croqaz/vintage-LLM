
# LLaMA 204,304,128 (204.30M) params
# huggingface/transformers/.. models/llama/configuration_llama.py

model_type = "llama"
hidden_size = 768          # embedding dimension
intermediate_size = 3072   # FFN hidden size (typically 4x hidden_size)
max_position_embeddings = 3072
num_attention_heads = 16   # attention heads (must divide hidden_size evenly)
num_hidden_layers = 18     # transformer layers
num_key_value_heads = 4
vocab_size = 32768
